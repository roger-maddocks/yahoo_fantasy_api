use crate::player::Player;

pub struct FantasyRoster {
    pub center_one: Player,
    pub center_two: Player,
    pub right_wing_one: Player,
    pub right_wing_two: Player,
    pub left_wing_one: Player,
    pub left_wing_two: Player,
    pub defenseman_one: Player,
    pub defenseman_two: Player,
    pub goaltender_one: Player,
    pub goaltender_two: Player,
    pub flex_one: Player,
    pub flex_two: Player,
    pub flex_three: Player,
    pub flex_four: Player,
}
